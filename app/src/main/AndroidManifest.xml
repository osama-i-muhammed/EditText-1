<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.duhok_tech.aqarat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DesingTEST" />
        <activity android:name=".Filter_House">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Filter_Aprt" />
        <activity android:name=".Filter_Villa" />
        <activity android:name=".Filter_Earth" />
        <activity android:name=".Filter_Farm" />
        <activity android:name=".Filter_Store" />
        <activity android:name=".Filter_Storage"></activity>
    </application>

</manifest>